requests>=2.31
numpy>=1.26
pillow>=10.0
static-ffmpeg>=2.5          # ffmpeg con libass si el sistema no lo tiene
faster-whisper>=1.0         # transcripción (Windows, Linux, Mac Intel)
mlx-whisper>=0.4 ; sys_platform == "darwin" and platform_machine == "arm64"   # Mac con chip Apple: GPU
opencv-python>=4.8          # editor profesional: imagen y detección de caras
scipy>=1.11                 # editor profesional: mezcla de sonido
torch>=2.2                  # editor profesional: superresolución y máscara de persona (sin él, va en modo básico)
